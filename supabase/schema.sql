-- Supabase Database Schema
-- Run this in your Supabase SQL Editor to set up the required tables and policies

-- Enable the "uuid-ossp" extension to generate UUIDs
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Basic schema ready for custom tables
-- Add your custom tables and policies here